// Shared doctest entry point. Compiled once; every test executable links
// this so the runner main() is not recompiled per translation unit.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
